name: Deploy to Development

on:
  push:
    branches: [develop]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: development

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build for development
        run: npm run build -- --configuration development

      - name: Deploy to development
        run: |
          echo "🚀 Deploying to development environment"
          echo "Build output: dist/resume-customizer-frontend/browser"
          # Add deployment commands here (e.g., rsync, scp, cloud CLI)
          # Example: aws s3 sync dist/resume-customizer-frontend/browser s3://dev-bucket
